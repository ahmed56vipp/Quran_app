name: quran_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/surah.json
    - assets/surah/
  fonts:
    - family: Uthmanic
      fonts:
        - asset: assets/ahmed.ttf
