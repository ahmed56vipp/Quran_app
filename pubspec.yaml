name: quran_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/quran_data/  # تأكد أن اسم المجلد هكذا
  fonts:
    - family: Uthmanic
      fonts:
        - asset: assets/ahmed.ttf
